import { products } from '@/content/products';
const groups={samsung:p=>p.brand==='Samsung',lg:p=>p.brand==='LG','lg-samsung':p=>['LG','Samsung'].includes(p.brand),brastemp:p=>p.brand==='Brastemp',electrolux:p=>p.brand==='Electrolux',hisense:p=>p.brand==='Hisense',conectadas:p=>p.wifi===true,ecobubble:p=>['samsung-ecobubble-digital-inverter','samsung-wd11a-ecobubble'].includes(p.slug),all:()=>true};
export function expandCatalog(markdown){return markdown.replace(/\{% catalog ([a-z-]+) %\}/g,(token,group)=>{if(!groups[group])throw Error(`Grupo de catálogo desconhecido: ${group}`);return ['| Modelo | Lavagem | Secagem | Referência |','| --- | --- | --- | --- |',...products.filter(groups[group]).map(p=>`| [${p.name}](/#${p.slug}) | ${p.capacityWash.toLocaleString('pt-BR')} kg | ${p.capacityDry} kg | [Fabricante](${p.sourceUrl}) · ${p.sourceChecked.split('-').reverse().join('/')} |`)].join('\n');});}
